// Lock In — local dev flag. Copy this file to env.js and flip the value.
// env.js is git-ignored, so the flag never ships and never shows in a diff.
// With it on, Ctrl+Shift+Enter runs a locked action without typing the challenge.
globalThis.LOCKIN_DEV = false;
